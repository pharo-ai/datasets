SmalltalkCISpec {
  #loading : [
    SCIMetacelloLoadSpec {
      #baseline : 'AIDatasets',
      #directory : 'src',
      #platforms : [ #pharo ],
      #onConflict : #useIncoming,
      #onUpgrade : #useIncoming
    }
  ],
  #testing : {
    #coverage : {
       #packages : [ 'AI-Datasets*' ],
       #format: #lcov
    }
  }
}
