SmalltalkCISpec {
  #loading : [
    SCIMetacelloLoadSpec {
      #baseline : 'AIDatasets',
      #directory : '',
      #onConflict : #useIncoming,
      #onUpgrade : #useIncoming,
      #ignoreImage : true
    }
  ],
  #testing : {
    #coverage : {
       #packages : [ 'AIDatasets*' ]
    }
}
